<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JavaApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.JavaApplication" parent="Base.Theme.JavaApplication" />

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Primary app color -->
        <item name="colorPrimary">#0BC88A</item>
        <!-- Color for the darker variant of colorPrimary -->
        <item name="colorPrimaryVariant">#03DAC5</item>
        <!-- Secondary app color -->
        <item name="colorSecondary">#03DAC5</item>
    </style>

</resources>